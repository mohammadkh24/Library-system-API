const express = require("express");
const usersController = require("../controllers/auth");

const router = express.Router();

// router.route("/").get(usersController.getAll).post(usersController.register)

module.exports = router